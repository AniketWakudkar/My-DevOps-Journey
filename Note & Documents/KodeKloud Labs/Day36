Deploy nginx(Alpine) server on App server


to do this task:
ssh to app server
docker pull nginx:alpine    //docker hub se pull karega
docker run -d --name nginx_3 -p 80:80 nginx_3

*run nginx -d = run nginx in background
--name = name the container nginx_3
expose port 80 on the server and 80 in the container*

docker ps   //to check if its running
docker images   //to check all images pulled 